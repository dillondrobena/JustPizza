<% provide(:title, "New Crust") %>
<h1>New Crust</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for @crust do |t| %>
        <%= render 'shared/crust_error_messages' %>

        <%= t.label :name %>
        <%= t.text_field :name, class: 'form-control'%>

        <%= t.submit "Create New Crust", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
